# file-containerization
A high-performance containerization and extraction tool designed to handle extremely large directory trees (millions of small files). It provides efficient filesystem traversal, chunked writes, and configurable fsync batching to optimize speed while ensuring data integrity at scale.
